module.exports = {
    content: [
      './templates/**/*.{html,js}',  // Asegúrate de que tu carpeta de templates esté aquí
      './static/**/*.{html,js}',     // O la ruta correcta donde están tus archivos HTML y JS
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };